# AI 自主开发约束

本仓库的需求分析、计划、实现、审查、测试、文档和交付证据均由 AI 执行。

## 执行规则

1. 先读取项目事实、现有实现和测试，再形成结论；不得用猜测补造业务规则。
2. 每个阶段由 AI 生成可复核证据，证据至少包含输入、命令或操作、结果、退出码和关联提交。
3. 验收、探索和审查必须转化为可重复的自动化检查，无法自动化的项目记为阻断项，不得以口头结论替代。
4. AI 完成实现后必须执行独立验证轮次；可使用独立代理，或在单代理环境中清空实现假设后按审查清单重新验证。
5. 不把开发工作转交给用户。仅在缺少不可推断的业务决策、外部凭据或权限、不可逆生产操作授权时请求外部授权，并记录范围与结果。
6. 必需门禁未通过、证据缺失或 `git diff` 未复核时，不得宣告完成。

## 仓库门禁

```bash
bash tests/test-scripts.sh
bash scripts/check-ai-autonomy.sh .
bash scripts/check-project.sh .
```
