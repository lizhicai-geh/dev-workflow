# 数据库迁移

## 原则

- 每次变更新增独立迁移，不修改已执行迁移。
- 大表操作评估锁、耗时和容量；删除字段前先移除旧代码依赖。
- 数据迁移可重试、可校验；提供对账、失败处理和恢复或补偿方案。

优先采用：`Expand → 新旧兼容 → Migrate → 验证 → Switch → Contract`。

## 必需信息

- 数据范围、数量、预计耗时和执行顺序；
- 是否可重复执行、断点续传以及错误数据处理；
- 一致性验证、对账和发布后观察方式；
- 失败恢复步骤；反向 SQL 不得作为唯一恢复手段。

高风险迁移需在接近生产的数据量上演练，并明确应用版本兼容窗口。
