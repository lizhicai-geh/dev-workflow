# 安全检查清单

只检查与变更相关的项目，不适用项说明原因。

- **设计**：敏感数据、访问角色、保留/删除、审计、租户隔离、越权/重放/篡改风险明确。
- **编码**：参数化查询、输入校验、输出转义和上传限制正确；服务端鉴权；无硬编码密钥或敏感日志。
- **供应链**：按风险启用密钥、依赖、静态代码、许可证和镜像扫描；CI 令牌最小权限。
- **运行**：测试/生产权限隔离；生产访问可审计；高风险操作确认；密钥可轮换；异常访问告警。

对发现项给出证据、影响、严重度和关闭条件；敏感证据必须脱敏。
