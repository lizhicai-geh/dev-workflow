# 任务拆分

将需求拆成可由 AI 独立开发、测试、验收和回滚的小批次，通常每项半天至两天。

每项任务包含：编号、单一目标、输入/输出、规则引用、依赖、范围/非目标、验收标准、自动验证方式、风险、复杂度和可并行性。

按需沿以下边界拆分：

1. 数据模型与迁移；
2. 领域规则、权限和数据范围；
3. API/契约、前端或第三方适配；
4. 异步/补偿、测试、监控和发布配置。

避免“开发前端/后端”“完成整个模块”、混入无关重构，或只能整体上线且无法独立验证的任务。
